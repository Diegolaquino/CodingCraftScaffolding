@model IEnumerable<CodingCraftHOMod1Ex2Scaffolding.Models.Categoria>

@{
    ViewBag.Title = "Indice";
}

<h2>Indice</h2>

<p>
    @Html.ActionLink("Criar Novo", "Criar")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Nome
        </th>
        <th>
            Produtos
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("Editar", "Editar", new { id=item.CategoriaId }) |
            @Html.ActionLink("Detalhes", "Detalhes", new { id=item.CategoriaId }) |
            @Html.ActionLink("Excluir", "Excluir", new { id=item.CategoriaId })
        </td>
        <td>
			@item.Nome
        </td>
        <td>
			@(item.Produtos == null ? "None" : item.Produtos.Count.ToString())
        </td>
    </tr>
}

</table>


